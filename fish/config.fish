if status is-interactive
    # Commands to run in interactive sessions can go here
end

alias snoop="python3 /home/boulder/Tools/snoop/snoop.py"
alias n="nvim"
# bun
set --export BUN_INSTALL "$HOME/.bun"
set --export PATH $BUN_INSTALL/bin $PATH

# bun
set --export BUN_INSTALL "$HOME/.bun"
set --export PATH $BUN_INSTALL/bin $PATH

# Created by `pipx` on 2024-06-18 21:05:01
set PATH $PATH /home/boulder/.local/bin
